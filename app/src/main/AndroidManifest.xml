<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ruby.hexamind.com.renticle2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".admin.AdminAddCarActivity"></activity>
        <activity android:name=".admin.AdminMainActivity" />
        <activity android:name=".Main.MainActivity" />
        <activity android:name=".PostLogin.PostLoginActivity" />
        <activity android:name=".Users.RegisterActivity" />
        <activity android:name=".ForgotPassword.ForgotPasswordActivity" />
        <activity android:name=".CarSelect.CarSelectActivity" />
        <activity
            android:name=".Users.LoginActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity
            android:name=".Start.StartActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>